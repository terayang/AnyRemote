{
  "name": "anyremote",
  "version": "0.1.2",
  "private": true,
  "scripts": {
    "dev": "wails dev",
    "build": "wails build",
    "typecheck": "go vet ./... && npm --prefix frontend run typecheck",
    "test": "go test ./...",
    "dist": "npm run dist:arm64 && npm run dist:x64",
    "dist:arm64": "wails build -platform darwin/arm64 -clean && bash scripts/build-dmg.sh build/bin/AnyRemote.app arm64",
    "dist:x64": "wails build -platform darwin/amd64 -clean && bash scripts/build-dmg.sh build/bin/AnyRemote.app x64",
    "dist:win": "wails build -platform windows/amd64 -nsis && bash scripts/rename-win-artifacts.sh"
  }
}
